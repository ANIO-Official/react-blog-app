//Navbar component


export default function Navbar(){
    return (
        <nav>
            <ul>
                {
                    /*Conditional Button: isAuthenticated -> Logout, !isAuthenticated -> Login */
                    
                }
            </ul>
        </nav>
    )
}